---
description: Run the Flutter application
---

Run the blocnet Flutter application.

Parameters:
- $1: Device ID or platform (optional). If not specified, run on the default device.
- $2: Additional flutter run arguments (optional).

Check available devices with `flutter devices` if no device is specified, then run the app on the appropriate device.
