---
description: Build the Flutter application for the specified platform
---

Build the blocnet Flutter application.

Parameters:
- $1: Platform to build for (android, ios, web, linux, macos, windows). Defaults to android if not specified.
- $2: Build mode (debug, profile, release). Defaults to debug if not specified.

Run the appropriate Flutter build command based on the parameters provided.
