---
description: Clean Flutter build artifacts and reinstall dependencies
---

Clean the Flutter project by:
1. Running `flutter clean` to remove build artifacts
2. Removing old dependency caches
3. Running `flutter pub get` to reinstall dependencies fresh

This is useful when experiencing build issues or after updating dependencies.
