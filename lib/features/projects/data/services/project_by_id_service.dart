import '../models/project_model.dart';

class ProjectByIdService {
  static Project fetchProjectById(String projectId) {
    // TODO: Implement fetching project from Firestore
    // Throwing exception for now - use Firestore queries instead of dummy data
    throw UnimplementedError(
      'ProjectByIdService.fetchProjectById needs to be implemented with Firestore queries',
    );
  }
}
